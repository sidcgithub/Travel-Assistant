<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable
            name="attraction"
            type="com.siddharthchordia.travelassistant.model.Attraction" />
    </data>
    <com.google.android.material.card.MaterialCardView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/item_padding_16dp"
        app:cardCornerRadius="@dimen/item_radius_16dp"
        app:cardElevation="4dp"
        app:cardBackgroundColor="?attr/colorSecondary"

        >


            <TextView
                android:background="@color/transparent"
                android:id="@+id/attraction_name"
                android:layout_gravity="center"
                android:padding="@dimen/attraction_padding_16dp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{attraction.name}"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="@color/attraction_card_text_color"/>


    </com.google.android.material.card.MaterialCardView>
</layout>
